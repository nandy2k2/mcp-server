# MongoDB connection string (required)
MONGODB_URI=mongodb+srv://user3:Hello123456@cluster0.bhzac.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# API key that Claude must send in x-api-key header (set any strong random string)
MCP_API_KEY=change-me-to-a-strong-random-secret

# JWT settings (must match your backend config.env)
JWT_SECRET=kumropatash-kuchu-pablo-posto-1980
JWT_EXPIRES_IN=200h

# Port (Azure sets this automatically, leave as 8000 locally)
PORT=8000
